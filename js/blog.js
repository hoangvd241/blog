var blog = (function () {
	'use strict';

	var
		configMap = {
			no_of_entries_per_load : 5,

			blog_model : null
		};


	var initModule = function ( $blog ) {

	};

	var loadMoreEntries = function ( $blog, starting_entry_index ) {
		
	};

	return { 
		initModule : initModule,
		loadMoreEntries : loadMoreEntries 
	};
} ());